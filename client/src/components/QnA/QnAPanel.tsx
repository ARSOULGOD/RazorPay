// Stretch Settlement Q&A chat panel — placeholder until Q&A layer is built.

export function QnAPanel() {
  return (
    <section style={{ marginTop: "2rem", color: "#666" }}>
      <h2 style={{ margin: "0 0 0.5rem" }}>Settlement Q&A</h2>
      <p style={{ margin: 0 }}>
        Stretch layer not implemented yet. Exception reasoning above is the
        source of truth for unresolved items.
      </p>
    </section>
  );
}
